# OpenStudio Hub — runtime dependencies
# Keep this minimal: only what the application imports at runtime.
# Versions are lower bounds, not exact pins.

# GUI toolkit (the PySide6 metapackage includes QtWebEngine, used by web_context_view)
PySide6>=6.6

# Kitsu production-tracker API (Gazu) — the single source of truth for Kitsu calls
gazu>=0.8

# HTTP client (KitsuManager, FileDownloader, widget_software, task_card, remote_explorer)
requests>=2.31

# Optional: HTTP Range streaming required for Watchtower video playback
RangeHTTPServer>=1.4
